defmodule PipelineRunServer.Request.Utilities do
  @moduledoc """
  Utility functions for the PipelineRunRequest controller.
  """

  def schedule_request(request) do
  end

  def archive_request(request) do
  end

  def cancel_request(request) do
  end

  def reschedule_request(request) do
  end
end
