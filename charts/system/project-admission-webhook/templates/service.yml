apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-admission-webhook.fullname" . }}
  labels:
    helm.sh/chart: {{ include "project-admission-webhook.chart" . }}
    app.kubernetes.io/name: {{ include "project-admission-webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: datapio
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "project-admission-webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
