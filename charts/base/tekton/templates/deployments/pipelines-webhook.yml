---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tekton.fullname" . }}-pipelines-webhook
  namespace: {{ .Release.Namespace }}
  labels:
    helm.sh/chart: {{ include "tekton.chart" . }}
    app.kubernetes.io/name: {{ include "tekton.name" . }}-pipelines-webhook
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: datapio
spec:
  replicas: {{ .Values.replicas.pipelines_webhook }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "tekton.chart" . }}-pipelines-webhook
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
      labels:
        app.kubernetes.io/name: {{ include "tekton.chart" . }}-pipelines-webhook
    spec:
      containers:
        - name: webhook
          image: {{ .Values.image.pipelines_webhook.name }}:{{ .Values.image.pipelines_webhook.tag }}
          imagePullPolicy: {{ .Values.image.pipelines_webhook.pullPolicy }}
          env:
            - name: SYSTEM_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          volumeMounts:
          - mountPath: /etc/config-logging
            name: config-logging
          resources:
      serviceAccountName: {{ include "tekton.fullname" . }}-pipelines-controller
      volumes:
        - configMap:
            name: config-logging
          name: config-logging
